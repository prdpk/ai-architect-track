[pytest]
# Only discover tests under tests/. Without this, pytest walks the whole repo
# and imports scripts/test_*.py (Phase 2 experiments) just to collect them —
# which builds ML models at import and made the suite take ~17s instead of ~0.1s.
testpaths = tests
